[pytest]
# 動態測試套件設定（2026-06-05）
#
# 99 個測試函式統一用 `return condition` 風格（if not X: return False）。
# 功能完全正確，但 pytest 8+ 會丟 PytestReturnNotNoneWarning。
# 重寫 99 個測試違反「最小代碼 + 精準修改」原則，
# 故採用 pytest.ini 全域過濾此類警告。
filterwarnings =
    ignore::pytest.PytestReturnNotNoneWarning
